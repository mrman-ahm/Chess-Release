Chess Release - Download and Play Instructions
=============================================

This game is a Windows release build.

Easiest Download
----------------

Download the complete ZIP here:

https://github.com/mrman-ahm/Chess-Release/releases/latest/download/Chess-Release-Windows.zip

Then:

1. Extract the ZIP file.
2. Open the RELEASE folder.
3. Double-click chess.exe.

Git Download
------------

If you want to clone the repo instead, it uses Git LFS for the large Stockfish engine file. Make sure Git LFS is installed before downloading the repo.

Requirements
------------

- Windows
- Git: https://git-scm.com/downloads
- Git LFS: https://git-lfs.com/

Commands
--------

Run these commands in PowerShell or Command Prompt:

git lfs install
git clone https://github.com/mrman-ahm/Chess-Release.git
cd Chess-Release
git lfs pull
cd RELEASE
.\chess.exe

Alternative Launch
------------------

After cloning and running "git lfs pull", you can also open the RELEASE folder and double-click chess.exe.

Updating Later
--------------

To download future updates:

cd Chess-Release
git pull
git lfs pull

Notes
-----

- Keep the files inside the RELEASE folder together. The game needs the included DLLs, audio, sprites, data, and Stockfish files.
- If chess.exe does not open, make sure Git LFS is installed and run "git lfs pull" again from inside the Chess-Release folder.
